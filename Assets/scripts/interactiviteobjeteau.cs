using UnityEngine;

public class interactiviteobjeteau : MonoBehaviour
{
    //**VOLET_3 : l'interaction avec l'eau (les objets qui vont interagir avec l'eau)
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
